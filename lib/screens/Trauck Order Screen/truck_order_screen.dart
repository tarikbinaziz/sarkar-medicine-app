import 'package:flutter/cupertino.dart';

class TruckOrderScreen extends StatelessWidget {
  const TruckOrderScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
